header
  #logo
    a(href="landing_01.html")
      img.logo_img(src="assets/images/which_method_logo.png", alt="")
      // <img ng-src="assets/images/which_method_logo.png" alt=""/>
      //img(ng-src='assets/images/which_method_logo.png', alt='')
  #menu
    a.logout_btn_01( ng-click='logout()') Log out
  // div(ng-include="'app/sections/header.html'")

  //-  div(ng-repeat="(key, value) in ranking" style="display:inline-block; width:170px")
    method: {{key}} pos: {{value.p}} neg: {{value.n}}


#container
  .body.questionpages
  h3 WhichMethod Section 2
  ul
    li#q4(ng-show="currentQuestion == 'q4'")
      //- li#q4(ng-show="currentSection == '2'")
      h4 When you are not using birth control, do you have:
      br
      label
        | Q4. Regular monthly periods?
      div(ng-repeat='option in questions.q4.options')
        input(type="radio" name='q4' ng-change="saveAnswers()" ng-model="questions.q4.selectedOption" ng-value="{{option}}")
        label.choice {{option.name}}
      input(type="button",ng-click="saveAnswers()",value="Skip")

    li#q4a(ng-show="currentQuestion == 'q4a'")
      //- li#q4(ng-show="currentSection == '2' && questions.q4.answer == 2 || questions.q4.answer =='2' ")
      h4 When you are not using birth control, do you have:
      br
      label
        | Q4a. Three or fewer periods per year?
      div(ng-repeat='option in questions.q4a.options')
        input(type="radio" name='q4a' ng-change="saveAnswers()" ng-model="questions.q4a.selectedOption" ng-value="{{option}}")
        label.choice {{option.name}}
      input(type="button",ng-click="saveAnswers()",value="Skip")

    li#q5(ng-show="currentQuestion == 'q5'")
      h4 When you are not using birth control, do you have:
      br
      label
        | Q5. Very heavy periods
      div(ng-repeat='option in questions.q5.options')
        input(type="radio" name='q5' ng-change="saveAnswers()" ng-model="questions.q5.selectedOption" ng-value="{{option}}")
        label.choice {{option.name}}
      input(type="button",ng-click="saveAnswers()",value="Skip")

    li#q6(ng-show="currentQuestion == 'q6'")
      h4 When you are not using birth control, do you have:
      br
      label
        | Q6. Periods that last longer than 7 days
      div(ng-repeat='option in questions.q6.options')
        input(type="radio" name='q6' ng-change="saveAnswers()" ng-model="questions.q6.selectedOption" ng-value="{{option}}")
        label.choice {{option.name}}
      input(type="button",ng-click="saveAnswers()",value="Skip")

    li#q7(ng-show="currentQuestion == 'q7'")
      h4 When you are not using birth control, do you have:
      br
      label
        | Q7. Painful periods or bad cramps
      div(ng-repeat='option in questions.q7.options')
        input(type="radio" name='q7' ng-change="saveAnswers()" ng-model="questions.q7.selectedOption" ng-value="{{option}}")
        label.choice {{option.name}}
      input(type="button",ng-click="saveAnswers()",value="Skip")

    li#q8(ng-show="currentQuestion == 'q8'")
      h4 When you are not using birth control, do you have:
      br
      label
        | Q8. Breast tenderness during your period
      div(ng-repeat='option in questions.q8.options')
        input(type="radio" name='q8' ng-change="saveAnswers()" ng-model="questions.q8.selectedOption" ng-value="{{option}}")
        label.choice {{option.name}}
      input(type="button",ng-click="saveAnswers()",value="Skip")

    li#q9(ng-show="currentQuestion == 'q9'")
      h4 When you are not using birth control, do you have:
      br
      label
        | Q9. Depression or anxiety during your period
      div(ng-repeat='option in questions.q9.options')
        input(type="radio" name='q9' ng-change="saveAnswers()" ng-model="questions.q9.selectedOption" ng-value="{{option}}")
        label.choice {{option.name}}
      input(type="button",ng-click="saveAnswers()",value="Skip")

    li#q10(ng-show="currentQuestion == 'q10'")
      h4 When you are not using birth control, do you have:
      br
      label
        | Q10. Bloating or fluid retention during your period
      div(ng-repeat='option in questions.q10.options')
        input(type="radio" name='q10' ng-change="saveAnswers()" ng-model="questions.q10.selectedOption" ng-value="{{option}}")
        label.choice {{option.name}}
      input(type="button",ng-click="saveAnswers()",value="Skip")

    li#q11(ng-show="currentQuestion == 'q11'")
      h4 When you are not using birth control, do you have:
      br
      label
        | Q11. Bad headaches with your period
      div(ng-repeat='option in questions.q11.options')
        input(type="radio" name='q11' ng-change="saveAnswers()" ng-model="questions.q11.selectedOption" ng-value="{{option}}")
        label.choice {{option.name}}
      input(type="button",ng-click="saveAnswers()",value="Skip")

    li#q12(ng-show="currentQuestion == 'q12'")
      h4 When you are not using birth control, do you have:
      br
      label
        | Q12. Significant PMS (premenstrual syndrome)
      div(ng-repeat='option in questions.q12.options')
        input(type="radio" name='q12' ng-change="saveAnswers()" ng-model="questions.q12.selectedOption" ng-value="{{option}}")
        label.choice {{option.name}}
      input(type="button",ng-click="saveAnswers()",value="Skip")

    li#q13(ng-show="currentQuestion == 'q13'")
      label
        | Q13. How often do these symptoms cause you to miss work or school?
      div(ng-repeat='option in questions.q13.options')
        input(type="radio" name='q13' ng-change="saveAnswers()" ng-model="questions.q13.selectedOption" ng-value="{{option}}")
        label.choice {{option.name}}
      input(type="button",ng-click="saveAnswers()",value="Skip")

    //- input(type="submit", ng-click="saveAnswers()", value="Continue")

// all questions above on a single page; Q4-Q13
//-   //this ends the second set of questions 02
div(ng-include="'app/sections/rankhelp.html'")

