header
    #logo
      a(href="/signup")
        img.logo_img(src="assets/images/which_method_logo.png", alt="", aria-label="Left Align")
    #menu
      a.logout_btn_01( ng-click='/logout()') Log out

//div(ng-include='"components/navbar/navbar.html"')
.signupColumn.leftColumn
  ul
    h3  SIGN UP TODAY
        span.glyphicon.glyphicon-check.btn-lg
        h5 There are two ways to use WhichMethod: <u>Registered Users</u> will receive up to 2 emails and will be able to save and email copy of Recommendations. <u>Guest Users</u> will be able to print recommendations, but must Register to email save.
    br
    h3 ANSWER THE QUESTIONS
        span.glyphicon.glyphicon-edit.btn-lg
        h5 WhichMethod asks questions about your medical history, preferences and priorities and sexual behavior in <kbd>Spanish</kbd> and <kbd>English</kbd>.
    br
    h3 GET RECOMMENDATIONS
        span.glyphicon.glyphicon-folder-open.btn-lg
        h5 WhichMethod identifies birth control methods that are <u>effective</u>, <u>safe</u> and <u>fit your needs</u>. Please share Recommendations with your doctor.


  
.signupColumn.rightColumn
  //- a(href="/")
    //- button.close &times;
  .row
    .col-sm-12
      h2 SIGN UP*for WhichMethod
    .col-sm-12
      form.form(name='form', ng-submit='register(form)', novalidate='')
        .form-group(ng-class='{ "has-success": form.name.$valid && submitted,\
        "has-error": form.name.$invalid && submitted }')
          //h3 Username
          //input.form-control(type='text', name='name', ng-model='user.name', required='')
          //p.help-block(ng-show='form.name.$error.required && submitted')
            //| A name is required

        .form-group(ng-class='{ "has-success": form.zip.$valid && submitted,\
        "has-error": form.zip.$invalid && submitted }')
          //h3 First three digits of your zip code
          //input.form-control(type='text', name='zip', ng-model='user.zip', required='',mongoose-error='')
          //p.help-block(ng-show='form.zip.$error.required && submitted')
            | First 3 digits of zipcode required
          p.help-block(ng-show='form.zip.$error.mongoose')
            | {{ errors.zip }}

        .form-group(ng-class='{ "has-success": form.email.$valid && submitted,\
        "has-error": form.email.$invalid && submitted }')
          h3 Email
          input.form-control(type='email', name='email', ng-model='user.email', required='', mongoose-error='')
          p.help-block(ng-show='form.email.$error.email && submitted')
            | Doesn't look like a valid email.
          p.help-block(ng-show='form.email.$error.required && submitted')
            | What's your email address?
          p.help-block(ng-show='form.email.$error.mongoose')
            | {{ errors.email }}

        .form-group(ng-class='{ "has-success": form.password.$valid && submitted,\
        "has-error": form.password.$invalid && submitted }')
          h3 Password
          input.form-control(type='password', name='password', ng-model='user.password', ng-minlength='3', required='', mongoose-error='')
          p.help-block(ng-show='(form.password.$error.minlength || form.password.$error.required) && submitted')
            | Password must be at least 3 characters.
          p.help-block(ng-show='form.password.$error.mongoose')
            | {{ errors.password }}

        div
          button.btn.btn-default.btn-lg.btn-login(type='submit')
            | Registered User

          = ' '
          a.btn.btn-default.btn-lg.btn-register(href='/login')
            | Log in
          br

          //a(href="javascript:void(0)" ng-click="guestSignin()") 
            | GUEST USER

          a.btn.btn-primary(data-toggle='collapse', ng-click="guestSignin()", aria-expanded='false', aria-controls='collapseExample')
            | Start as Guest User
            br
        div
            h5 <mark>* By signing up you agree to WhichMethod.org's Privacy Policy</mark>


  hr